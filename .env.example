# Copy this file to `.env` and fill in values. Never commit real keys.
OPENAI_API_KEY=
# Optional: override default secret concept (server-only, never sent to client)
LOGIOS_SECRET=Eternity
# Optional
# OPENAI_MODEL=gpt-4o-mini
# PORT=3333
