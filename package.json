{
  "name": "ikaros",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "qa:install-browsers": "playwright install chromium",
    "qa:gameplay": "node scripts/qa-gameplay.mjs",
    "stamp:build": "node scripts/release-stamp.mjs",
    "release:notion": "node scripts/release-notion.mjs",
    "build": "npm run stamp:build && npm run release:notion && vite build",
    "preview": "node server.js --prod"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "vite": "^5.4.8"
  }
}
