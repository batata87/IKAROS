{
  "name": "ikaros",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "stamp:build": "node scripts/release-stamp.mjs",
    "build": "npm run stamp:build && vite build",
    "preview": "node server.js --prod"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  }
}
